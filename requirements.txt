# 依赖真相源已收口到 pyproject.toml。此文件仅为薄 shim，保持 `pip install -r` 可用。
#
# 首次环境搭建：
# 1. conda env create -f environment.yml
# 2. conda activate multi-agent
# 3. pip install -r requirements.txt          # = pip install -e .（运行时依赖）
# 4. cd scripts/node && npm install && cd ../..   # App Store 爬取（Node.js）
#
# 可选扩展（按需）：
#   pip install -e ".[dev]"        # 开发/测试工具
-e .
