# CLI：
# conda env create -f environment.yml
# conda activate multi-agent
# pip install -r requirements.txt
# pip install -e .                      # 安装 cca 包本身，让 scripts/ 下脚本可 import cca
# python -m spacy download zh_core_web_sm

# LangGraph
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langgraph-checkpoint-sqlite>=2.0.0

# models
openai>=1.40.0
langchain-openai>=0.2.0
langchain-deepseek>=0.1.0

# Doubao (Volcengine Ark) - 用作最终跨模型 QA
# LLM chat 仍走 ChatOpenAI + base_url（OpenAI 兼容），SDK 为图像生成/embedding 等高级特性预留
volcengine-python-sdk>=1.0.0

# State
pydantic>=2.7.0
pyyaml>=6.0
python-dotenv>=1.0.0

# information collection
tavily-python>=0.5.0
requests>=2.32.0
httpx>=0.27.0                # Collector fetch_url 的 HTTP 客户端（支持 follow_redirects + 超时）
beautifulsoup4>=4.12.0
lxml>=5.0.0
trafilatura>=2.0.0           # fetch_url 网页正文提取（比 BeautifulSoup 干净，自动剥 nav/广告/脚本）
pypdf>=4.0.0
pymupdf>=1.24.0
pillow>=10.0.0

# Data Analysis
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.8.0            # chart.py 图表渲染

# PII 脱敏
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0

# front
streamlit>=1.36.0
plotly>=5.20.0

# 报告 PDF 渲染（保留：答辩演示需要 PDF 形态）
# 渲染链：weasyprint（Linux/Mac）→ reportlab 直出（Windows 主路径，无需 GTK）
markdown-it-py>=3.0.0
weasyprint>=62.0             # Linux/Mac；Windows 安装失败时代码自动 fallback 到 reportlab
reportlab>=4.0.0             # Windows PDF 渲染主路径（weasyprint 依赖 GTK，Windows 不自带）

# 可观测性
langsmith>=0.1.0
loguru>=0.7.0
tenacity>=8.2.0

# === 开发期依赖 (dev only) ===
# 备注：未拆 requirements-dev.txt，直接合并以降低 reviewer 认知成本。
# 生产/演示部署时可手动跳过这一段，不会影响主流程。
ruff>=0.5.0                 # lint + format 一体（替代 black + flake8 + isort）
pre-commit>=3.7.0           # 提交前自动跑 ruff，挡格式问题

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
