# CLI：
# conda env create -f environment.yml
# conda activate multi-agent
# python -m spacy download zh_core_web_sm
# pip install -r requirements.txt

# LangGraph
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langgraph-checkpoint-sqlite>=2.0.0

# models
openai>=1.40.0
langchain-openai>=0.2.0
langchain-deepseek>=0.1.0

# State 
pydantic>=2.7.0
pyyaml>=6.0
python-dotenv>=1.0.0

# information collection
tavily-python>=0.5.0
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pypdf>=4.0.0
pymupdf>=1.24.0
pillow>=10.0.0

# Data Analysis
pandas>=2.2.0
numpy>=1.26.0

# PII 脱敏
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0

# front
streamlit>=1.36.0
plotly>=5.20.0

# 报告 PDF 渲染（保留：答辩演示需要 PDF 形态）
# 路径：markdown -> html (markdown-it-py) -> pdf (weasyprint)
# 若 weasyprint 在 Windows 安装失败，可改为 xhtml2pdf（纯 Python，CSS 支持弱）
markdown-it-py>=3.0.0
weasyprint>=62.0

# 可观测性
langsmith>=0.1.0
loguru>=0.7.0
tenacity>=8.2.0

# === 开发期依赖 (dev only) ===
# 备注：未拆 requirements-dev.txt，直接合并以降低 reviewer 认知成本。
# 生产/演示部署时可手动跳过这一段，不会影响主流程。
ruff>=0.5.0                 # lint + format 一体（替代 black + flake8 + isort）
pre-commit>=3.7.0           # 提交前自动跑 ruff，挡格式问题

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
