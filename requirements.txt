# 运行 Agent 系统所需的全部依赖
#
# 安装（首次环境搭建）：
# 1. conda env create -f environment.yml
# 2. conda activate multi-agent
# 3. pip install -r requirements.txt
# 4. pip install -e .
# 5. cd scripts/node && npm install && cd ../..   # App Store 爬取（Node.js）
#
# 队友拉新代码后只需：
# pip install -r requirements.txt
# cd scripts/node && npm install && cd ../..
#
# 可选扩展：
# pip install -r requirements-finetune.txt  # BERT 离线微调（需要大量数据才有效）
# pip install -r requirements-dev.txt       # 开发/测试工具

# LangGraph
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langgraph-checkpoint-sqlite>=2.0.0

# LLM 客户端
openai>=1.40.0
langchain-openai>=0.2.0
langchain-deepseek>=0.1.0
volcengine-python-sdk>=1.0.0      # Doubao（火山方舟）

# 基础工具
pydantic>=2.7.0
pyyaml>=6.0
python-dotenv>=1.0.0

# 信息采集
tavily-python>=0.5.0              # web_search
requests>=2.32.0
httpx>=0.27.0                     # fetcher
beautifulsoup4>=4.12.0
lxml>=5.0.0                       # trafilatura 解析后端
trafilatura>=2.0.0                # fetcher 正文提取
pypdf>=4.0.0                      # pdf_reader 文档抽取

# 数据分析
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.8.0                 # 图表渲染
scikit-learn>=1.4.0               # Insight NMF 主题提取（cca/utils/nlp_utils.py）

# NLP 推理（Insight BERT 情感分类；CPU 即可，无需 GPU）
# transformers 推理路径同时也是 bert_finetune 训练的依赖；torch 是 transformers backend
transformers>=4.40.0
torch>=2.1.0

# 前端
streamlit>=1.36.0

# 报告渲染
markdown-it-py>=3.0.0             # pdf_renderer
weasyprint>=62.0                  # Linux/Mac
reportlab>=4.0.0                  # Windows

# 可观测性
langsmith>=0.1.0
loguru>=0.7.0
tenacity>=8.2.0
