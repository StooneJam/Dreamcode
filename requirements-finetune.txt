# BERT 离线微调专用依赖
#
# 安装：pip install -r requirements-finetune.txt
#
# 使用场景：
#   收集好 5000+ 条领域评论样本后，运行：
#   python scripts/finetune_bert.py
#
# 注意：微调是一次性离线操作，不在 Agent 请求路径内执行。
# 微调完成后 Agent 会自动使用本地模型（无需修改代码）。

datasets>=2.18.0   # HuggingFace Trainer 数据集构建（bert_finetune/train.py）
